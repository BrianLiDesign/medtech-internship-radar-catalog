# Issue tracker

**Tracker:** local markdown under `.scratch/<feature>/`

Wayfinder maps live as `map.md`. Child tickets are markdown files under `.scratch/<feature>/issues/`.

Blocking is expressed in ticket front matter (`blocked_by:`) when needed. The frontier is open, unblocked tickets.
