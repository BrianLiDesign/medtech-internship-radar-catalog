# Loop state

**v1:** T6.2 signed off (locked 12 + archive/refresh).  
**Wave 1 / 2:** allowlist signed off — 32 device companies.  
**Adapters (24/32):** Boston Scientific / Dexcom (Eightfold PCSX), Penumbra (Lever), Inspire Medical (Greenhouse), Abbott / Zimmer Biomet / GE HealthCare / STERIS / CONMED / Philips / Siemens Healthineers (Phenom `/widgets`), Intuitive (SmartRecruiters), CooperCompanies / Hologic (Oracle Recruiting CE), BD / Baxter (TalentBrew), Align (Pinpoint), Teleflex / Olympus / Arthrex (jobs2web search HTML), J&J MedTech (internships landing cards), Stryker (Paradox `/jobs?keyword=intern` preload JSON), Edwards (Algolia InstantSearch `EdwardsCareersJobs`), Integra LifeSciences (Kentico `/api/jobs/search` intern HTML). Live J&J intern cards, Stryker intern-keyword hits, and Edwards intern-keyword hits on 2026-08-15/16 were non-US (or non-STEM). Integra intern keyword search was empty on 2026-08-16. Production catalog is unchanged until daily refresh.

**Next:** research public JSON or confirmed public listings for the remaining 8. Do **not** reverse-engineer Workday. Integra intern keyword listing is Kentico `/api/jobs/search` HTML (adapter shipped; live intern hits were empty). Edwards jobsearch InstantSearch POSTs to Algolia index `EdwardsCareersJobs`. Do not treat Edwards `/api/jobs` HTML shells as a listing feed. Workday `/{site}/rss` and `/{site}/sitemap.xml` return Jobs Hub HTML shells. Smith+Nephew Sitecore job-search APIs 404. Alcon/Insulet/Biotronik `jsonapi/node/job` 404. Globus WP jobs CPT 404. Tandem `/api/jobs` 404; internship-program Next.js page has no job JSON (Search Internships → Workday). Remaining-company Eightfold PCSX hosts 404 or connection-fail. Paradox `/jobs?keyword=intern` and Kentico `/api/jobs/search` on remaining marketing hosts 404 (Tandem `/jobs` is a Workday widget redirect, not a listing). Remaining Phenom `/widgets` on marketing origins 404. Remaining SmartRecruiters company slugs are empty `totalFound=0` stubs. Remaining iCIMS hosts DNS-resolve but boards are decommissioned (`gone`/`dc409`). Remaining Ashby boards are not found. Workable widgets for Medtronic/Alcon return empty `jobs: []` stubs. Insulet co-op reqs appear on third-party aggregators but the student hub still only links Workday `insuletcareers` (no job cards). Remaining Lever/Recruitee/Teamtailor/Greenhouse job-board slugs 404. Remaining JazzHR/BambooHR slugs are inactive or marketing redirects. Dayforce `jobs.dayforcehcm.com` guessed portals return the same empty SPA shell. Avature/Rippling remaining hosts connection-fail. Edwards-style `/careers/jobsearch` paths on remaining hubs 404 or redirect to the marketing careers page (no Algolia). UltiPro guessed sites 404. Medtronic CSOD careersite goes to `error.aspx`. Paylocity guessed recruiting boards redirect to JobNotFound. ADP Workforce Now guessed CIDs 404. ResMed WP pages `search=intern` is empty. Medtronic intern window is open (Aug 1–Oct 13); Summer 2027 intern reqs are live on Workday and university aggregators, but the early-careers page still only links Workday `jobFamilyGroup` search (no job cards / Algolia / Phenom). `jobs.medtronic.com` has a hostname-mismatch cert. Do not scrape university Handshake mirrors or Workday CXS. Alcon `/early-careers/` still only `SEARCH OPEN ROLES` → Workday. Biotronik SuccessFactors career portal is `loginFlowRequired`; MSEI `mst.com` job offers use the same SuccessFactors company. Production Biotronik/MSEI jobs2web hosts still DNS-fail; do not scrape staging jobs2web. ResMed early-careers `Discover Jobs` links are Workday `q=` searches (`techintern`, regional early-careers tags), not a listing feed. Remaining: Medtronic, Insulet, Tandem, Smith+Nephew, ResMed, Globus Medical, Biotronik, Alcon.

**Stop expanding the allowlist** unless new researched **device** intern hubs appear. Do not add hospitals, pharma, or Big Tech health.
